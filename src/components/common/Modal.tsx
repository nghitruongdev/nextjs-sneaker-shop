import {
  useDisclosure,
  Box,
  Button,
  Modal,
  ModalOverlay,
  ModalContent,
  ModalHeader,
  ModalCloseButton,
  ModalBody,
  ModalFooter,
} from '@chakra-ui/react'
import React, { ReactNode } from 'react'
import { ReactElement } from 'react'

type Props = {
  modalProps?: {}
}
